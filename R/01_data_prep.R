# Ingest raw data from the source location.
# Returns a data frame of raw response data.
ingest_data <- function() {
  # TODO: implement data ingestion
  data.frame()
}

# Score raw responses.
# raw_data: data frame returned by ingest_data()
# Returns a data frame of scored responses.
score_responses <- function(raw_data) {
  # TODO: implement response scoring
  raw_data
}
